export default class LoginsModel {
    constructor(displayName, role, token, memberId) {
        this.displayName = displayName;
        this.role = role;
        this.token = token;
        this.memberId = memberId;
    }
}
